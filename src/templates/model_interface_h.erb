/*
 * @brief:  <%= @interface.name%>
 * @author: APIEASY
 * @date:   <%= Time.new().strftime('%Y-%m-%d')%>
 */

#import <Foundation/Foundation.h>
#import "XQRequest.h"
#import "<%= @model_name %>Parameter.h"

@interface <%= @model_name %> : XQRequest

- (id)initWithParam:(<%= @model_name %>Parameter*)param;

@property (nonatomic, strong) <%= @model_name %>Parameter* param;

@end
