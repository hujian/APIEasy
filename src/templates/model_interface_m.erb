#import "<%= @model_name %>.h"

@implementation <%= @model_name %>

- (id)initWithParam:(<%= @model_name %>Parameter*)param {
  self = [super init];

  if (nil != self) {
      self.param = param;
  }

  return self;
}

- (NSString *)requestUrl {
    return @"<%= @interface.path_develop %>";
}

- (YTKRequestMethod)requestMethod {
    <% if @interface.http_type == 'GET' %>
    return YTKRequestMethodGET;
    <% else %>
    return YTKRequestMethodPOST;
    <% end %>
}

@end
